import Foundation

enum AppConstants {
    static let appName = "Pólen"
    static let cloudKitContainerIdentifier = "iCloud.Lorran.Polen"
    static let googleBooksBaseURL = URL(string: "https://www.googleapis.com/books/v1")!
}
